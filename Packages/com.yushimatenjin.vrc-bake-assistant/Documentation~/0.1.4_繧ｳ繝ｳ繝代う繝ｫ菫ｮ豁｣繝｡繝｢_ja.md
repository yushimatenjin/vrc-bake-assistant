# VRC Bake Assistant 0.1.4 コンパイル修正メモ

## 修正内容

Unity 2022.3.22f1で以下のコンパイルエラーが出る問題を修正しました。

```text
error CS1503: Argument 1: cannot convert from 'UnityEngine.GUIContent' to 'int'
```

原因は `EditorGUILayout.IntPopup` に `GUIContent` ラベルと `string[]` 選択肢を同時に渡していたことです。
Unity 2022.3ではこの組み合わせのオーバーロードに合わないため、ラベル表示と `IntPopup` 本体を分けました。

## 該当箇所

```csharp
using (new EditorGUILayout.HorizontalScope())
{
    EditorGUILayout.LabelField(new GUIContent("Reflection Probe解像度", "反射の解像度です。まずは128、重い場合は64。"), GUILayout.Width(170));
    reflectionProbeResolution = EditorGUILayout.IntPopup(reflectionProbeResolution,
        new[] { "64", "128", "256", "512" },
        new[] { 64, 128, 256, 512 });
}
```

## 更新手順

1. 既存リポジトリへ上書き
2. `git add .`
3. `git commit -m "Fix IntPopup compile error for 0.1.4"`
4. `git push`
5. GitHub Pagesをデプロイ
6. ALCOMでリポジトリ更新
